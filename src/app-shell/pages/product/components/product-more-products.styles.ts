import * as stylex from "@stylexjs/stylex";

export const productMoreProductsStyles = stylex.create({
  boundary: {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    minHeight: 80,
    gap: 12,
  },
});
