import * as stylex from "@stylexjs/stylex";

export const searchFiltersLoadingPageStyles = stylex.create({
  page: {
    display: "grid",
    gridTemplateRows: "auto 1fr",
    minHeight: "100dvh",
  },
});
